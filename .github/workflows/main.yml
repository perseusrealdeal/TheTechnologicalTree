# .github/workflows/main.yml
name: Build & Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
#  schedule:
#    - cron: '30 5 * * 5'

jobs:

  build:
    runs-on: macos-15
    steps:
    - name: Checkout the code
      uses: actions/checkout@v4
    - name: Select Xcode 16.2
      run: sudo xcode-select -s "/Applications/Xcode_16.2.app"
    - name: Show buildable schemes
      run: xcodebuild -list -project ./T3Project.xcodeproj
    - uses: mxcl/xcodebuild@v3
      with:
        platform: macOS
        scheme: 'T3Project'
        action: build
#        working-directory: ./DarkModeDiscovery
        verbosity: xcpretty
        upload-logs: always
        configuration: Release

  unit-tests:
    runs-on: macos-15
    steps:
    - name: Checkout the code
      uses: actions/checkout@v4
#    - name: SwiftLint
#        run: brew link --overwrite swiftlint || brew install swiftlint
    - name: Select Xcode 16.2
      run: sudo xcode-select -s "/Applications/Xcode_16.2.app"
    - name: Show buildable schemes
      run: xcodebuild -list -project ./T3Project.xcodeproj
    - uses: mxcl/xcodebuild@v3
      with:
        platform: macOS
        scheme: 'T3Project'
        action: test
#        working-directory: ./DarkModeDiscovery
        verbosity: xcpretty
        upload-logs: always
        configuration: Release
